language: rust

rust:
  - stable
  - beta
  - nightly

matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true

branches:
  only:
    # release tags
    - /^v\d+\.\d+\.\d+.*$/
    - master

